[package]
name = "danger-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.11"
anyhow = "1.0"
danger-kernel = { path = "../kernel", artifact = "bin", target = "x86_64-unknown-none" }
clap = { version = "4.0", features = ["derive"] }
