[package]
name = "danger-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader_api = "0.11"
linked_list_allocator = "0.10"
uart_16550 = "0.3"
x86_64 = "0.15"
# We will add no_std compatible crates later for inference
# For now, minimal bootable kernel
